"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const BN = require("bn.js");
const hex_1 = require("../hex");
function anyToBN(data = 0, options = {}) {
    options = Object.assign({ defaults: null }, options);
    let result = options.defaults;
    switch (typeof data) {
        case "number":
            result = new BN(data, 10);
            break;
        case "string":
            const hex = hex_1.prepareHex(data);
            if (hex) {
                result = new BN(hex, 16);
            }
            break;
        case "object":
            if (BN.isBN(data)) {
                result = data;
            }
            else if (Buffer.isBuffer(data)) {
                result = new BN(data.toString("hex"), 16);
            }
            break;
    }
    return result;
}
exports.anyToBN = anyToBN;
